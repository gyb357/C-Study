#include <stdio.h>
/*
void print_stars();

int main() {
	print_stars();

	printf("\n");
	printf("hello World\n");

	print_stars();

	return 0;
}

void print_stars() {
	for (int i = 1; i <= 30; i++) {
		printf("*");
	}
}
*/

/*
int max(int x, int y);

int main() {
	int n1, n2;

	printf("정수 입력\n");
	scanf("%d, %d", &n1, &n2);

	printf("더 큰 정수 = %d", max(n1, n2));

	return 0;
}

int max(int x, int y) {
	if (x > y) {
		return x;
	} else {
		return y;
	}
}
*/

/*
int scanf();
int prime(int n);

int main() {
	if (prime(scanf()) == 2) {
		printf("소수 아님");
	} else {
		printf("소수");
	}

	return 0;
}

int scanf() {
	int n1;

	printf("정수 입력\n");
	scanf("%d", &n1);

	return n1;
}

int prime(int n) {
	int asdf = 0;

	for (int i = 1; i <= n; i++) {
		if (n % i == 0) {
			asdf++;
		}
	}

	if (asdf == 2) {
		return 1;
	} else {
		return 2;
	}
}
*/

/*
int scanf();
void return_all_prime_num(int x);

int main() {

	return_all_prime_num(scanf());

	return 0;
}

int scanf() {
	int n1;

	printf("정수 입력\n");
	scanf("%d", &n1);

	return n1;
}


void return_all_prime_num(int x) {
	int asdf = 0;

	for (int i = x; i < 100; i++) {
		if (x % i == 0) {
			asdf++;
		}
	}

	if (asdf == 2) {
		printf("i\n");
	}
}
*/
