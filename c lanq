#include <stdio.h>

int main() {
	int a;

	 printf("C 언어 및 실습 20192718 고영보\n");

	 // printf("Number: ");
	 // scanf("%d", &a);

	 // printf("Int = %s", a > 0 ? "양수" : (a == 0 ? "영" : "음수"));

	// ---------- //

	 //a = 1;

	 // a = a + 1;
	 // printf("a << 1 = %d\n", a);

	 // a = a + 1;
	 // printf("a << 1 = %d\n", a);

	 // a = a + 1;
	 // printf("a << 1 = %d\n", a);

	// ---------- //

	// int i, goal;

	// goal = 100;

	// for (i = 0; i <= goal; ++i) {
	//  	printf("i = %d\n", i);
	// }

	// i++, ++i 전자: i를 나중에 더함, 후자: 출력 전 먼저 더함

	// ---------- //

	// int i, j;

	// printf("1.5(%d) + 1.8(%d) = %d", 1.5, 1.8, (int)1.5 + (int)1.8);
}
